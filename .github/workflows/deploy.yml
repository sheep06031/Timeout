name: Deploy to EC2 (self-hosted)

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Run deploy script on EC2
        run: |
          bash /srv/timeout/app/deploy.sh
